import { ProviderCheck } from '@/components/ProviderCheck';import { OFFICIAL_SOURCES } from '@/lib/sources';
export const metadata={title:'Check a college or provider'};
export default function Page(){return <div className="container section"><div className="pagehead"><div><div className="eyebrow">Protect your future</div><h1>Check a college or training provider</h1><p className="muted" style={{maxWidth:820}}>Search before you pay. LifePath only shows a green verified status when a record has a recognised verification source. Not finding a name is a warning to investigate further, not an accusation of fraud.</p></div></div><ProviderCheck/><div style={{marginTop:34}}><div className="section-head"><div><h2>Official sources LifePath uses</h2></div></div><div className="grid grid-2">{OFFICIAL_SOURCES.map(s=><a className="card" href={s.url} target="_blank" rel="noreferrer" key={s.name}><span className="pill good">Official source</span><h3 style={{marginTop:10}}>{s.name}</h3><p className="muted">{s.purpose}</p></a>)}</div></div></div>}
