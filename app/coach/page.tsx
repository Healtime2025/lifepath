import { AppShell } from '@/components/AppShell';import { CoachPanel } from '@/components/CoachPanel';import { currentUser } from '@/lib/auth';import { redirect } from 'next/navigation';
export const metadata={title:'AI career coach'};
export default async function Page(){const u=await currentUser().catch(()=>null);if(!u)redirect('/login');return <AppShell><div className="pagehead"><div><div className="eyebrow">AI-assisted guidance</div><h1>Career coach</h1><p className="muted">Personalised explanations grounded in your LifePath profile. It should help you investigate choices, not make the choice for you.</p></div></div><div className="notice" style={{marginBottom:18}}>Institution, accreditation, funding and application facts should come from LifePath’s verified database or official links. The coach is instructed not to invent them.</div><CoachPanel/></AppShell>}
