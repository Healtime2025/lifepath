import { db } from '@/lib/db';import { SubjectPlanner } from '@/components/SubjectPlanner';
export const metadata={title:'Subject choice planner'};
export default async function Page(){const sql=db();const careers=await sql`SELECT slug,title,category,summary,subject_guidance FROM careers WHERE active=true ORDER BY title`;return <div className="container section"><div className="pagehead"><div><div className="eyebrow">Grade 9–11 subject decisions</div><h1>See the doors before you close them.</h1><p className="muted" style={{maxWidth:850}}>Subject choices can affect later study routes. LifePath helps learners investigate the impact early, while there is still time to adjust.</p></div></div><div className="notice" style={{marginBottom:20}}>LifePath does not say a learner “cannot” have a career from one school subject alone. It highlights routes that deserve a requirements check before a subject is changed.</div><SubjectPlanner careers={careers}/></div>}
