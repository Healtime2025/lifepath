import { requireUser } from '@/lib/auth';import { db } from '@/lib/db';import { bad,cleanText,ok } from '@/lib/http';
export async function POST(req:Request){try{const u=await requireUser();const b=await req.json();const first=cleanText(b.firstName,80),last=cleanText(b.lastName,80),grade=cleanText(b.grade,40),province=cleanText(b.province,60),school=cleanText(b.schoolName,160),highest=cleanText(b.highestGradeCompleted,60);if(!first||!grade)return bad('First name and current grade are required.');const sql=db();await sql`INSERT INTO learner_profiles(user_id,first_name,last_name,grade,province,school_name,highest_grade_completed,onboarding_complete,updated_at) VALUES(${u.id},${first},${last},${grade},${province},${school},${highest},true,now()) ON CONFLICT(user_id) DO UPDATE SET first_name=EXCLUDED.first_name,last_name=EXCLUDED.last_name,grade=EXCLUDED.grade,province=EXCLUDED.province,school_name=EXCLUDED.school_name,highest_grade_completed=EXCLUDED.highest_grade_completed,onboarding_complete=true,updated_at=now()`;return ok({ok:true});}catch(e:any){return bad(e.message==='UNAUTHENTICATED'?'Sign in required.':'Could not save profile.',e.message==='UNAUTHENTICATED'?401:500)}}
