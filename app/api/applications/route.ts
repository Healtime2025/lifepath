import { requireUser } from '@/lib/auth';import { db } from '@/lib/db';import { bad,cleanText,cleanUrl,ok } from '@/lib/http';
const allowedTypes=new Set(['study','funding','learnership','apprenticeship','employment','other']);const allowedStatuses=new Set(['planned','started','submitted','awaiting','offered','accepted','declined','closed']);
export async function POST(req:Request){try{const u=await requireUser();const b=await req.json();const title=cleanText(b.title,200),type=cleanText(b.applicationType,30),deadline=cleanText(b.deadline,20)||null,url=cleanUrl(b.externalUrl)||null,notes=cleanText(b.notes,2000)||null;if(!title||!allowedTypes.has(type))return bad('Title and valid application type are required.');const sql=db();const rows=await sql`INSERT INTO applications(user_id,title,application_type,external_url,deadline,notes) VALUES(${u.id},${title},${type},${url},${deadline},${notes}) RETURNING *`;await sql`INSERT INTO progress_events(user_id,event_type,title,metadata) VALUES(${u.id},'application_added','Application added',${JSON.stringify({title,type})}::jsonb)`;return ok({application:rows[0]});}catch(e:any){return bad(e.message==='UNAUTHENTICATED'?'Sign in required.':'Could not save application.',e.message==='UNAUTHENTICATED'?401:500)}}
export async function PATCH(req:Request){try{const u=await requireUser();const b=await req.json();const id=cleanText(b.id,60),status=cleanText(b.status,30);if(!id||!allowedStatuses.has(status))return bad('Invalid update.');const sql=db();const rows=await sql`UPDATE applications SET status=${status},updated_at=now() WHERE id=${id}::uuid AND user_id=${u.id} RETURNING *`;if(!rows.length)return bad('Application not found.',404);return ok({application:rows[0]});}catch(e:any){return bad('Could not update application.',500)}}
export async function DELETE(req:Request){try{const u=await requireUser();const id=(new URL(req.url).searchParams.get('id')||'').slice(0,60);const sql=db();await sql`DELETE FROM applications WHERE id=${id}::uuid AND user_id=${u.id}`;return ok({ok:true});}catch{return bad('Could not remove application.',500)}}
