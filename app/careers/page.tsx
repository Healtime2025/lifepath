import { db } from '@/lib/db';import { CareerExplorer } from '@/components/CareerExplorer';
export const metadata={title:'Explore careers'};
export default async function Page(){const sql=db();const rows=await sql`SELECT id,slug,title,category,summary,pathways FROM careers WHERE active=true ORDER BY title`;return <div className="container section"><div className="pagehead"><div><div className="eyebrow">Explore</div><h1>Careers in every direction</h1><p className="muted">Explore by career family instead of facing one endless list. Every route remains open to investigate.</p></div></div><CareerExplorer careers={rows}/></div>}
