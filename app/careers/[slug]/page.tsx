import Link from 'next/link';import { db } from '@/lib/db';import { notFound } from 'next/navigation';import { PathwayTags } from '@/components/PathwayTags';import { SaveCareerButton } from '@/components/SaveCareerButton';import { currentUser } from '@/lib/auth';import { latestMarks } from '@/lib/learner';
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const sql=db();const rows=await sql`SELECT * FROM careers WHERE slug=${slug} AND active=true LIMIT 1`;const c:any=rows[0];if(!c)notFound();const u=await currentUser().catch(()=>null);const marks=u?await latestMarks(u.id):{};const types=[] as string[];if((c.pathways||[]).some((p:string)=>['university','university_of_technology'].includes(p)))types.push('university');if((c.pathways||[]).includes('tvet'))types.push('tvet');const inst=types.length?await sql`SELECT * FROM institutions WHERE active=true AND verification_status='verified' AND institution_type=ANY(${types}) ORDER BY public_institution DESC,name LIMIT 12`:[];return <div className="container section"><Link href="/careers" className="muted">← All careers</Link><div className="pagehead" style={{marginTop:18}}><div><span className="pill">{c.category}</span><h1 style={{marginTop:10}}>{c.title}</h1><p className="muted" style={{fontSize:18,maxWidth:760,lineHeight:1.6}}>{c.summary}</p></div>{u?<SaveCareerButton careerId={c.id}/>:<Link className="btn btn-primary" href="/register">Start my LifePath</Link>}</div><div className="grid grid-2"><div className="card"><h3>What you would do</h3><p className="muted" style={{lineHeight:1.7}}>{c.what_you_do}</p><h3 style={{marginTop:22}}>Routes into this career</h3><div style={{marginTop:12}}><PathwayTags items={c.pathways}/></div><p className="muted" style={{fontSize:13,lineHeight:1.55,marginTop:15}}>Routes vary by employer, qualification and regulatory requirements. LifePath shows alternatives so university is never assumed to be the only route.</p></div><div className="card"><h3>School subject guidance</h3>{Object.keys(c.subject_guidance||{}).length?<div className="timeline" style={{marginTop:15}}>{Object.entries(c.subject_guidance).map(([s,importance])=><div className="timeline-item" key={s}><span className="dot">{marks[s]!=null?Math.round(marks[s]):'•'}</span><div><b>{s}</b><div className="muted" style={{fontSize:13}}>{String(importance)}{marks[s]!=null?` · your latest mark: ${marks[s]}%`:''}</div></div></div>)}</div>:<p className="muted">No single school subject defines this route. Specific programmes may have their own entry requirements.</p>}<div className="notice" style={{marginTop:18}}>Always compare the exact requirements of the qualification and institution you plan to apply to.</div></div></div>{inst.length>0&&<div style={{marginTop:24}}><div className="section-head"><div><h2>Verified public institutions to research</h2><p className="muted">These institutions are verified in the official public directory. This does not mean every institution below offers this exact career programme — open the official site and confirm the programme.</p></div></div><div className="grid grid-3">{inst.map((i:any)=><div className="card" key={i.id}><span className="pill good">✓ Verified</span><h3 style={{marginTop:10}}>{i.name}</h3><p className="muted">{i.province} · {i.institution_type==='tvet'?'TVET College':'University'}</p><a className="btn btn-ghost" href={i.website_url} target="_blank" rel="noreferrer">Official website ↗</a></div>)}</div></div>}</div>}
