import { AppShell } from '@/components/AppShell'; import { OnboardingForm } from '@/components/OnboardingForm'; import { ReportUpload } from '@/components/ReportUpload'; import { currentUser } from '@/lib/auth'; import { db } from '@/lib/db'; import { redirect } from 'next/navigation';
export const metadata={title:'My profile'};
export default async function Page(){const u=await currentUser().catch(()=>null);if(!u)redirect('/login');const sql=db();const [p,m]=await Promise.all([sql`SELECT * FROM learner_profiles WHERE user_id=${u.id} LIMIT 1`,sql`SELECT DISTINCT ON(subject) subject,mark FROM learner_marks WHERE user_id=${u.id} ORDER BY subject,recorded_at DESC`]);return <AppShell><div className="pagehead"><div><div className="eyebrow">My LifePath</div><h1>Tell us where you are now</h1><p className="muted">Your grade, subjects and marks help us show routes that are open and gaps you can still work on.</p></div></div><ReportUpload grade={p[0]?.grade}/><div className="card"><OnboardingForm profile={p[0]} marks={m}/></div></AppShell>}
