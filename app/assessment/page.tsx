import { AppShell } from '@/components/AppShell';import { AssessmentWizard } from '@/components/AssessmentWizard';import { currentUser } from '@/lib/auth';import { redirect } from 'next/navigation';
export const metadata={title:'Discovery assessment'};
export default async function Page(){const u=await currentUser().catch(()=>null);if(!u)redirect('/login');return <AppShell><div className="pagehead"><div><div className="eyebrow">Discover me</div><h1>What kind of work feels like you?</h1><p className="muted">There are no right or wrong answers. This is not a label or a test you can fail — it helps widen the careers worth exploring.</p></div></div><div className="notice" style={{marginBottom:18}}>LifePath uses six work-interest dimensions rather than assigning you a fixed personality type. Your interests, marks and preferences can change over time.</div><AssessmentWizard/></AppShell>}
