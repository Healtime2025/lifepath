import { AppShell } from '@/components/AppShell';import { ApplicationsBoard } from '@/components/ApplicationsBoard';import { currentUser } from '@/lib/auth';import { db } from '@/lib/db';import { redirect } from 'next/navigation';
export const metadata={title:'Applications'};
export default async function Page(){const u=await currentUser().catch(()=>null);if(!u)redirect('/login');const sql=db();const rows=await sql`SELECT * FROM applications WHERE user_id=${u.id} ORDER BY deadline NULLS LAST,updated_at DESC`;return <AppShell><div className="pagehead"><div><div className="eyebrow">Apply & track</div><h1>Keep every application moving</h1><p className="muted">Study, funding, learnerships and apprenticeships can all be tracked in one place.</p></div></div><ApplicationsBoard initial={rows}/></AppShell>}
