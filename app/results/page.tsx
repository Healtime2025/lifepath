import Link from 'next/link';import { AppShell } from '@/components/AppShell';import { currentUser } from '@/lib/auth';import { redirect } from 'next/navigation';import { latestAssessment,latestMarks,topCareerMatches } from '@/lib/learner';import { dimensions } from '@/lib/assessment';import { ResultsCareerFamilies } from '@/components/ResultsCareerFamilies';
export const metadata={title:'My results'};
export default async function Page(){const u=await currentUser().catch(()=>null);if(!u)redirect('/login');const[a,marks,matches]=await Promise.all([latestAssessment(u.id),latestMarks(u.id),topCareerMatches(u.id)]);if(!a)redirect('/assessment');return <AppShell><div className="pagehead"><div><div className="eyebrow">My LifePath profile</div><h1>Your strengths are a starting point.</h1><p className="muted">LifePath narrows the catalogue, explains why possibilities appeared, and keeps every career family available to explore.</p></div><Link className="btn btn-soft" href="/assessment">Retake assessment</Link></div>
<div className="grid grid-3" style={{marginBottom:24}}>{dimensions.map(([k,label,desc])=><div className="card" key={k}><div style={{display:'flex',justifyContent:'space-between',gap:10}}><h3>{label}</h3><b style={{color:'var(--brand)'}}>{Number(a.scores[k]||0)}%</b></div><div className="progress" style={{margin:'12px 0'}}><span style={{width:`${Number(a.scores[k]||0)}%`}}/></div><p className="muted" style={{fontSize:13,lineHeight:1.55}}>{desc}</p></div>)}</div>
<div className="section-head"><div><div className="eyebrow">Worth exploring</div><h2>Your career families</h2><p className="muted">Strong alignment is not a probability or a decision. Open a family to investigate the possibilities and routes.</p></div><Link className="btn btn-ghost" href="/careers">Explore every career</Link></div><ResultsCareerFamilies matches={matches}/>
{Object.keys(marks).length===0&&<div className="notice" style={{marginTop:20}}>Add your latest subjects and marks so LifePath can also show academic and subject-route signals. <Link href="/onboarding" style={{fontWeight:800}}>Add marks â†’</Link></div>}</AppShell>}

