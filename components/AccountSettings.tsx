'use client';
import { useState } from 'react';import { useRouter } from 'next/navigation';
export function AccountSettings(){const[confirmText,setConfirmText]=useState('');const[busy,setBusy]=useState(false);const router=useRouter();async function remove(){if(confirmText!=='DELETE')return;setBusy(true);const r=await fetch('/api/account/delete',{method:'DELETE'});setBusy(false);if(r.ok){router.push('/');router.refresh()}}return <div className="card"><h3>Delete my LifePath account</h3><p className="muted">This permanently deletes your account and linked learner data from the LifePath database, including marks, assessment results, saved careers and application tracking.</p><div className="field"><label>Type DELETE to confirm</label><input className="input" value={confirmText} onChange={e=>setConfirmText(e.target.value)} style={{maxWidth:260}}/></div><button className="btn btn-danger" disabled={confirmText!=='DELETE'||busy} onClick={remove}>{busy?'Deleting…':'Delete account permanently'}</button></div>}
