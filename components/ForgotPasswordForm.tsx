'use client';
import { FormEvent,useState } from 'react';
export function ForgotPasswordForm(){const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);async function go(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const fd=new FormData(e.currentTarget);await fetch('/api/auth/forgot-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:fd.get('email')})});setBusy(false);setMsg('If an account exists for that email, a reset link has been sent when email delivery is configured.')}return <form className="form" onSubmit={go}><div className="field"><label>Email</label><input className="input" type="email" name="email" required/></div>{msg&&<div className="notice">{msg}</div>}<button className="btn btn-primary" disabled={busy}>{busy?'Sending…':'Send reset link'}</button></form>}
