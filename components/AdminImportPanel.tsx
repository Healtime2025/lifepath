'use client';
import { FormEvent,useState } from 'react';
export function AdminImportPanel(){const[busy,setBusy]=useState(false);const[msg,setMsg]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMsg('');const fd=new FormData(e.currentTarget);let records:any;try{records=JSON.parse(String(fd.get('records')||'[]'))}catch{setBusy(false);setMsg('JSON is invalid.');return}const body={importType:fd.get('importType'),sourceName:fd.get('sourceName'),sourceUrl:fd.get('sourceUrl'),records};const r=await fetch('/api/admin/import',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const d=await r.json().catch(()=>({}));setBusy(false);setMsg(r.ok?`Imported ${d.upserted} of ${d.total} records.`:(d.error||'Import failed.'))}return <form className="card form" onSubmit={submit}><div className="grid grid-2"><div className="field"><label>Import type</label><select className="select" name="importType"><option value="institutions">Institutions / providers</option><option value="qualifications">Qualifications</option><option value="programmes">Institution programmes</option><option value="funding">Funding opportunities</option></select></div><div className="field"><label>Source name</label><input className="input" name="sourceName" required placeholder="e.g. DHET Private Higher Education Register"/></div></div><div className="field"><label>Official source URL</label><input className="input" name="sourceUrl" type="url" placeholder="https://…"/></div><div className="field"><label>JSON records</label><textarea className="textarea" name="records" required style={{minHeight:280,fontFamily:'ui-monospace,monospace'}} placeholder={'[\n  { "name": "Example", "slug": "example", ... }\n]'}/></div>{msg&&<div className="notice">{msg}</div>}<button className="btn btn-primary" disabled={busy}>{busy?'Importing…':'Validate and import'}</button></form>}
